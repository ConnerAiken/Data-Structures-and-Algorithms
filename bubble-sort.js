var unsortedArray = [
    1, 9, 5, 7, 3, 5, 1, 9, 8, 2, 5, 4
];
 
bubbleSort(unsortedArray);


function bubbleSort(sourceArr) {
    let tmp1, tmp2;
    var returnArr = [];
    
    console.log("Starting Bubble Sort - source array: "+sourceArr.join(','));
    
    sourceArr.forEach((item, index) => {
        if(index >= 1) { 
        }

        console.log(''); 
        
        if(item[i] > item[i+1]) { 
            item[i+1] = item[i];
            item[i] = tmp1; 
        } 
    });
}
  